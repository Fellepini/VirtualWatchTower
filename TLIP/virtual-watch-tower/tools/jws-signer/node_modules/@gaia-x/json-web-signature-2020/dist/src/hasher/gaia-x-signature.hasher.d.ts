/// <reference types="node" />
import { VerifiableCredential } from '../model/verifiable-credential.model';
import { Hasher } from './hasher';
export declare class GaiaXSignatureHasher extends Hasher {
    hash(document: Omit<VerifiableCredential, 'proof'>): Promise<Buffer | Uint8Array>;
}
